const target=new Date('2025-09-19T00:00:00+07:00').getTime();function pad(n){return n.toString().padStart(2,'0')}
function updateCountdown(id){const el=document.getElementById(id);if(!el)return;let diff=target-Date.now();if(diff>=0){const d=Math.floor(diff/86400000),h=Math.floor(diff%86400000/3600000),m=Math.floor(diff%3600000/60000),s=Math.floor(diff%60000/1000);el.textContent=`${d} hari ${pad(h)} jam ${pad(m)} menit ${pad(s)} detik`}else{diff=Math.abs(diff);const d=Math.floor(diff/86400000),h=Math.floor(diff%86400000/3600000),m=Math.floor(diff%3600000/60000),s=Math.floor(diff%60000/1000);el.textContent=`+${d} hari ${pad(h)} jam ${pad(m)} menit ${pad(s)} detik`}}setInterval(()=>updateCountdown('countdown'),1000);updateCountdown('countdown');
const audio=document.getElementById('music'),playBtn=document.getElementById('playMusic');if(playBtn&&audio){let playing=false;playBtn.addEventListener('click',()=>{if(!playing){audio.play();playBtn.textContent='⏸️ Jeda Musik'}else{audio.pause();playBtn.textContent='▶️ Putar Musik'}playing=!playing})}